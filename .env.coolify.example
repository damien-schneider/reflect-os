# =============================================================================
# Coolify Environment Variables Template
# =============================================================================
# Copy this file to set up your Coolify deployment environment variables.
# All variables are validated at runtime using t3-env + Zod schemas.
# See apps/web/src/env/ for validation rules.
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (Required)
# -----------------------------------------------------------------------------
# PostgreSQL credentials for the database container
POSTGRES_USER=user
POSTGRES_PASSWORD=your-secure-database-password
POSTGRES_DB=postgres

# -----------------------------------------------------------------------------
# ZERO SYNC SERVER (Required)
# -----------------------------------------------------------------------------
# Secret key for Zero JWT authentication
# IMPORTANT: Must be at least 32 characters in production!
# Generate with: openssl rand -base64 32
ZERO_AUTH_SECRET=your-zero-auth-secret-minimum-32-characters

# Zero cache server log level (debug | info | warn | error)
ZERO_LOG_LEVEL=info

# Number of Zero sync workers (adjust based on load)
ZERO_NUM_SYNC_WORKERS=2

# -----------------------------------------------------------------------------
# AUTHENTICATION (Required)
# -----------------------------------------------------------------------------
# Secret key for Better Auth session encryption
# IMPORTANT: Must be at least 32 characters in production!
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-better-auth-secret-minimum-32-characters

# Base URL for Better Auth callbacks (your app's public URL)
BETTER_AUTH_URL=https://app.yourdomain.com

# -----------------------------------------------------------------------------
# PUBLIC URLS (Required - exposed to browser)
# -----------------------------------------------------------------------------
# URL where the Zero sync server is accessible
# This is used by the browser to connect via WebSocket
VITE_PUBLIC_ZERO_SERVER=https://zero.yourdomain.com

# URL where the API server is accessible
# This is used by the browser for API calls
VITE_PUBLIC_API_SERVER=https://app.yourdomain.com

# -----------------------------------------------------------------------------
# OPTIONAL: Development Overrides
# -----------------------------------------------------------------------------
# Skip environment validation (NOT recommended for production)
# SKIP_ENV_VALIDATION=true

# Node environment (set automatically by docker-compose)
# NODE_ENV=production
