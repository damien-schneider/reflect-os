# =============================================================================
# Reflet - QUICK START ENVIRONMENT VARIABLES
# =============================================================================
#
# This is a consolidated reference of ALL environment variables.
# For better organization, copy the relevant sections to per-app .env files:
#   - apps/backend/.env  → Backend API server
#   - apps/web/.env      → Frontend web app
#
# See apps/backend/.env.example and apps/web/.env.example for app-specific templates.
# =============================================================================

# =============================================================================
# DATABASE & ZERO SYNC (Required for backend)
# =============================================================================
ZERO_UPSTREAM_DB="postgresql://user:password@127.0.0.1:5430/postgres"
ZERO_AUTH_SECRET="your-zero-auth-secret-min-32-chars-in-prod"
ZERO_REPLICA_FILE="/tmp/reflect_zero_replica.db"
ZERO_LOG_LEVEL="debug"

# =============================================================================
# AUTHENTICATION (Required for backend)
# =============================================================================
BETTER_AUTH_SECRET="your-better-auth-secret-min-32-chars-in-prod"
BETTER_AUTH_URL="http://localhost:5173"

# =============================================================================
# EMAIL (Optional - uses test mode without)
# =============================================================================
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=""
# Use onboarding@resend.dev for development (Resend's test domain)
EMAIL_FROM_ADDRESS="onboarding@resend.dev"
EMAIL_FROM_NAME="Reflet"

# =============================================================================
# BILLING (Optional)
# =============================================================================
# Get tokens from https://polar.sh - Sandbox and Production are separate accounts!
POLAR_ACCESS_TOKEN=""
POLAR_ENVIRONMENT="sandbox"
# POLAR_WEBHOOK_SECRET=""  # Created when setting up webhooks in Polar dashboard

# =============================================================================
# CLIENT-SIDE (Required for web)
# =============================================================================
VITE_PUBLIC_ZERO_SERVER="http://localhost:4848"

# =============================================================================
# DEPLOYMENT (Optional - for SST/AWS)
# =============================================================================
# AWS_REGION="us-east-1"
# DOMAIN_NAME="your-domain.com"
# DOMAIN_CERT="arn:aws:acm:..."
