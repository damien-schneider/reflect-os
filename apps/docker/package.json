{
  "name": "@repo/docker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "docker compose --env-file ../../.env -f ./docker-compose.yaml up -d && ./wait-for-pg.sh",
    "start": "docker compose --env-file ../../.env -f ./docker-compose.yaml up -d",
    "stop": "docker compose --env-file ../../.env -f ./docker-compose.yaml down",
    "logs": "docker compose --env-file ../../.env -f ./docker-compose.yaml logs -f",
    "clean": "docker compose --env-file ../../.env -f ./docker-compose.yaml down -v"
  }
}
