# =============================================================================
# WEB APP - Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values.
# Only VITE_PUBLIC_* variables are exposed to the browser!
# =============================================================================

# =============================================================================
# CLIENT-SIDE (Exposed to browser)
# =============================================================================

# URL of the Zero sync server (WebSocket endpoint)
# Development: http://localhost:4848
# Production: https://zero.yourdomain.com
VITE_PUBLIC_ZERO_SERVER="http://localhost:4848"

# =============================================================================
# SERVER-SIDE (SSR only - not exposed to browser)
# =============================================================================

# Internal backend API URL for server-side proxy
# Development: http://localhost:3001
# Docker: http://backend:3001
INTERNAL_API_URL="http://localhost:3001"

# Port for the web server (default: 3000 in production, 5173 in dev)
# PORT=3000

# Node environment: development | production | test
# NODE_ENV="development"
